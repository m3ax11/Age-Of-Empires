on_actions = {
    on_war_relation_added = {
        effect = {
            if = {
                limit = { ROOT = { OR = { original_tag = GER original_tag = FRA original_tag = SOV original_tag = ITA original_tag = ENG original_tag = USA original_tag = JAP original_tag = CHI } } }
                ROOT = { set_country_flag = AOEIW_final_major_war_fought }
            }
            if = {
                limit = { FROM = { OR = { original_tag = GER original_tag = FRA original_tag = SOV original_tag = ITA original_tag = ENG original_tag = USA original_tag = JAP original_tag = CHI } } }
                FROM = { set_country_flag = AOEIW_final_major_war_fought }
            }
            if = {
                limit = {
                    ROOT = { original_tag = JAP }
                    FROM = { original_tag = CHI }
                    NOT = { has_global_flag = AOEIW_final_JAP_CHI_reaction_seen }
                }
                set_global_flag = AOEIW_final_JAP_CHI_reaction_seen
                ROOT = { add_timed_idea = { idea = AOEIW_final_JAP_overextension days = 365 } }
                if = { limit = { USA = { exists = yes } } USA = { country_event = { id = aoeiw_final_japan.10 days = 3 } } }
                if = { limit = { SOV = { exists = yes } } SOV = { country_event = { id = aoeiw_final_japan.11 days = 4 } } }
            }
            if = {
                limit = {
                    ROOT = { original_tag = CHI }
                    FROM = { original_tag = JAP }
                    NOT = { has_global_flag = AOEIW_final_JAP_CHI_reaction_seen }
                }
                set_global_flag = AOEIW_final_JAP_CHI_reaction_seen
                FROM = { add_timed_idea = { idea = AOEIW_final_JAP_overextension days = 365 } }
                if = { limit = { USA = { exists = yes } } USA = { country_event = { id = aoeiw_final_japan.10 days = 3 } } }
                if = { limit = { SOV = { exists = yes } } SOV = { country_event = { id = aoeiw_final_japan.11 days = 4 } } }
            }
        }
    }
}
