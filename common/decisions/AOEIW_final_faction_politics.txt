AOEIW_final_faction_politics = {
    AOEIW_final_reichspakt_command_conference = {
        icon = generic_military
        cost = 50
        days_re_enable = 365
        allowed = { original_tag = GER }
        visible = { is_in_faction_with = HOL }
        available = { has_war = no }
        complete_effect = { country_event = { id = aoeiw_final_factions.1 hours = 6 } }
        ai_will_do = { base = 4 }
    }
    AOEIW_final_reichspakt_economic_protocol = {
        icon = generic_construction
        cost = 75
        days_re_enable = 365
        allowed = { original_tag = GER }
        visible = { is_in_faction_with = HOL }
        available = { has_war = no }
        complete_effect = {
            every_country = {
                limit = { is_in_faction_with = GER }
                add_timed_idea = { idea = AOEIW_final_faction_economic_protocol days = 240 }
                add_opinion_modifier = { target = GER modifier = AOEIW_final_faction_coordination }
            }
        }
        ai_will_do = { base = 3 }
    }
    AOEIW_final_old_order_security_council = {
        icon = generic_military
        cost = 50
        days_re_enable = 365
        allowed = { original_tag = FRA }
        visible = { is_in_faction_with = BEL }
        available = { has_war = no }
        complete_effect = { country_event = { id = aoeiw_final_factions.2 hours = 6 } }
        ai_will_do = { base = 4 }
    }
    AOEIW_final_old_order_economic_compact = {
        icon = generic_construction
        cost = 75
        days_re_enable = 365
        allowed = { original_tag = FRA }
        visible = { is_in_faction_with = BEL }
        available = { has_war = no }
        complete_effect = {
            every_country = {
                limit = { is_in_faction_with = FRA }
                add_timed_idea = { idea = AOEIW_final_faction_economic_protocol days = 240 }
                add_opinion_modifier = { target = FRA modifier = AOEIW_final_faction_coordination }
            }
        }
        ai_will_do = { base = 3 }
    }
    AOEIW_final_mediterranean_strategy_conference = {
        icon = generic_naval
        cost = 50
        days_re_enable = 365
        allowed = { original_tag = ITA }
        visible = { OR = { is_in_faction_with = ROM is_in_faction_with = YUG is_in_faction_with = BUL } }
        available = { has_war = no }
        complete_effect = { country_event = { id = aoeiw_final_factions.3 hours = 6 } }
        ai_will_do = { base = 4 }
    }
    AOEIW_final_mediterranean_economic_board = {
        icon = generic_construction
        cost = 75
        days_re_enable = 365
        allowed = { original_tag = ITA }
        visible = { OR = { is_in_faction_with = ROM is_in_faction_with = YUG is_in_faction_with = BUL } }
        available = { has_war = no }
        complete_effect = {
            every_country = {
                limit = { is_in_faction_with = ITA }
                add_timed_idea = { idea = AOEIW_final_faction_economic_protocol days = 240 }
                add_opinion_modifier = { target = ITA modifier = AOEIW_final_faction_coordination }
            }
        }
        ai_will_do = { base = 3 }
    }

    AOEIW_final_POL_petition_for_autonomy = {
        icon = generic_political_discourse
        cost = 75
        fire_only_once = yes
        allowed = { original_tag = POL }
        visible = { is_subject_of = GER }
        available = {
            has_war = no
            has_stability > 0.45
        }
        complete_effect = { GER = { country_event = { id = aoeiw_final_factions.10 days = 2 } } }
        ai_will_do = { base = 3 }
    }
}
